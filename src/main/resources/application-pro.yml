#生产环境
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog?useUnicode=true&characterEncoding=utf-8&mybatisTimezone=GMT&useSSL=false&serverTimezone=UTC
    username: root
    password: 18309293959
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

logging:
  level:
    root: warn
    com.example.blog: info
  file:
    name: log/blog-pro.log
server:
  port: 8081
